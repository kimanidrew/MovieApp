export const playerStyles = `
.netflix-player-wrapper { position: relative; width: 100%; height: 100%; background: #000; overflow: hidden; } .netflix-player-wrapper.hide-cursor { cursor: none; } .netflix-video { width: 100%; height: 100%; object-fit: contain;filter: contrast(1.15) brightness(1.05) saturate(1.1); image-rendering: auto; backface-visibility: hidden; transform: translateZ(0); } .hover-scale { transition: transform 0.2s; cursor: pointer; } .hover-scale:hover { transform: scale(1.15); } .skip-anim-container { position: absolute; top: 0; bottom: 0; width: 40%; display: flex; flex-direction: column; align-items: center; justify-content: center; opacity: 0; background: radial-gradient(circle, rgba(255,255,255,0.1) 0%, transparent 70%); transition: opacity 0.3s; pointer-events: none; color: white; z-index: 12; } .skip-anim-container.active { opacity: 1; } .skip-anim-container.left { left: 0; border-top-right-radius: 50% 100%; border-bottom-right-radius: 50% 100%; } .skip-anim-container.right { right: 0; border-top-left-radius: 50% 100%; border-bottom-left-radius: 50% 100%; } .skip-icon-wrapper { display: flex; gap: 2px; font-size: 2.5rem; margin-bottom: 5px; } .skip-text { font-size: 0.9rem; font-weight: bold; } .active .arrow-one { animation: ghosting 0.6s infinite; } .active .arrow-two { animation: ghosting 0.6s infinite 0.1s; } .active .arrow-three { animation: ghosting 0.6s infinite 0.2s; } @keyframes ghosting { 0% { opacity: 0; } 50% { opacity: 1; } 100% { opacity: 0; } } .netflix-header { position: absolute; top: 0; left: 0; right: 0; height: 120px; background: linear-gradient(to bottom, rgba(0,0,0,0.8), transparent); display: flex; align-items: flex-start; padding: 2rem 4%; opacity: 0; transition: opacity 0.4s; z-index: 20; pointer-events: none; } .netflix-header.visible { opacity: 1; pointer-events: auto; } .back-button { color: white; background: transparent; border: none; } .header-title { padding-left: 1rem; color: white; font-size: 1.6rem; font-weight: 600; text-shadow: 0 2px 4px rgba(0,0,0,0.5); } .resume-toast { position: absolute; bottom: 120px; left: 30px; background: rgba(20,20,20,0.95); color: white; padding: 12px 20px; border-radius: 8px; z-index: 40; border: 1px solid rgba(255,255,255,0.1); animation: slideUpFade 0.5s ease forwards; backdrop-filter: blur(10px); } @keyframes slideUpFade { from { transform: translateY(50px); opacity: 0; } to { transform: translateY(0); opacity: 1; } } .resume-yes { background: #e50914; color: white; border: none; padding: 6px 14px; border-radius: 4px; font-weight: bold; cursor: pointer; margin-right: 10px; } .resume-no { background: none; border: none; color: white; cursor: pointer; font-size: 1.2rem; } .skip-intro-container { position: absolute; bottom: 140px; right: 0; z-index: 50; display: flex; flex-direction: column; align-items: flex-end; gap: 8px; animation: slideInRight 0.5s ease-out forwards; } @keyframes slideInRight { from { transform: translateX(100%); opacity: 0; } to { transform: translateX(-40px); opacity: 1; } } .skip-btn { background: rgba(255, 255, 255, 0.1); border: 1px solid rgba(255, 255, 255, 0.4); color: white; padding: 10px 30px; font-size: 1rem; font-weight: bold; text-transform: uppercase; cursor: pointer; backdrop-filter: blur(5px); transition: 0.2s; } .skip-btn:hover { background: rgba(255, 255, 255, 0.2); } .auto-skip-toggle { display: flex; align-items: center; gap: 6px; font-size: 0.75rem; color: rgba(255,255,255,0.6); cursor: pointer; } .center-action-overlay { position: absolute; top: 50%; left: 50%; transform: translate(-50%, -50%); z-index: 15; pointer-events: none; } .center-icon-bg { background: rgba(0,0,0,0.5); border-radius: 50%; width: 100px; height: 100px; display: flex; align-items: center; justify-content: center; backdrop-filter: blur(5px); } .animate-pop { animation: popFade 0.5s ease-out forwards; } @keyframes popFade { 0% { transform: scale(0.8); opacity: 0; } 50% { transform: scale(1.1); opacity: 1; } 100% { transform: scale(1); opacity: 0; } } .gesture-hud { position: absolute; top: 50%; left: 50%; transform: translate(-50%, -50%); background: rgba(0,0,0,0.7); padding: 20px; border-radius: 12px; z-index: 50; display: flex; flex-direction: column; align-items: center; pointer-events: none; } .gesture-hud-icon { font-size: 2rem; margin-bottom: 8px; } .gesture-hud-bar { width: 100px; height: 4px; background: rgba(255,255,255,0.2); border-radius: 2px; overflow: hidden; } .gesture-hud-bar div { height: 100%; background: #e50914; transition: width 0.1s linear; } .netflix-controls { position: absolute; bottom: 0; left: 0; right: 0; background: linear-gradient(to top, rgba(0,0,0,0.9), transparent); padding: 2rem 4% 1.5rem; box-sizing: border-box; opacity: 0; transition: opacity 0.4s; pointer-events: none; z-index: 20; } .netflix-controls.visible { opacity: 1; pointer-events: auto; } .progress-container { position: relative; height: 4px; background: rgba(255,255,255,0.2); border-radius: 2px; cursor: pointer; transition: height 0.2s; margin-bottom: 1rem; } .progress-container:hover { height: 8px; } .progress-slider { position: absolute; left: 0; right: 0; width: 100%; opacity: 0; z-index: 10; cursor: pointer; } .progress-fill { position: absolute; height: 100%; background: #e50914; border-radius: 2px; display: flex; align-items: center; justify-content: flex-end; } .buffer-bar { position: absolute; height: 100%; background: rgba(255,255,255,0.35); border-radius: 2px; } .scrub-circle { width: 16px; height: 16px; background: #e50914; border-radius: 50%; transform: translateX(50%) scale(0); transition: transform 0.2s ease; box-shadow: 0 0 5px rgba(0,0,0,0.5); } .progress-container:hover .scrub-circle { transform: translateX(50%) scale(1); } .controls-row { display: flex; justify-content: space-between; align-items: center; min-width: 0; } .controls-left, .controls-right { display: flex; align-items: center; gap: 1.5rem; min-width: 0; } .control-btn { background: none; border: none; color: white; cursor: pointer; transition: transform 0.2s; } .control-btn svg { width: 38px; height: 38px; } .volume-container { display: flex; align-items: center; gap: 0.8rem; } .volume-slider { width: 0; opacity: 0; transition: width 0.3s, opacity 0.3s; height: 4px; accent-color: #e50914; cursor: pointer; border: none;} .volume-container:hover .volume-slider { width: 100px; opacity: 1; } .custom-quality-container { position: relative; display: flex; align-items: center; } .quality-trigger { background: rgba(20,20,20,0.8); color: white; border: 1px solid rgba(255,255,255,0.3); padding: 3px 10px; border-radius: 4px; font-size: 0.85rem; cursor: pointer; display: flex; align-items: center; gap: 5px; } .chevron { width: 20px; transition: transform 0.3s; } .chevron.open { transform: rotate(180deg); } .quality-menu { position: absolute; bottom: 100%; left: 0; margin-bottom: 10px; background: rgba(20,20,20,0.95); border-radius: 8px; overflow: hidden; display: flex; flex-direction: column; width: 140px; border: 1px solid rgba(255,255,255,0.1); } .quality-menu button { background: none; border: none; color: rgba(255,255,255,0.7); padding: 10px; text-align: left; cursor: pointer; } .quality-menu button.active { color: #e50914; font-weight: bold; } .animate-spin { animation: spin 1s linear infinite; } @keyframes spin { to { transform: rotate(360deg); } }
`;